import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.appUsers.bayteq',
  appName: 'app-users',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
